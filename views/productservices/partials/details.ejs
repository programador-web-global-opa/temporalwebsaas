<div class="container-fluid px-0">

  <div class="secondary-header mb-3 p-2 rounded-3">
    <h5 class="mb-0">Estado de Solicitudes</h5>
  </div>
  <p class="text-muted mb-3">
    Lista de solicitudes realizadas ordenadas de la más nueva a la más antigua.
    Da clic en <strong>Ver más</strong> para ver el historial de estados.
  </p>

  <div class="table-responsive">
    <table class="table table-bordered align-middle">
      <thead class="table-light">
        <tr>
          <th class="text-center">N. Solicitud</th>
          <th class="text-center">Producto</th>
          <th class="text-center">F. Solicitud</th>
          <th class="text-center">Último estado</th>
          <th class="text-center">Ver más</th>
        </tr>
      </thead>
      <tbody id="tbody-solicitudes">
        <tr id="fila-cargando">
          <td colspan="5" class="text-center text-muted py-4">Cargando solicitudes...</td>
        </tr>
      </tbody>
    </table>
  </div>

</div>

<%# Modal seguimiento %>
<div class="modal fade app-modal" id="modalSeguimiento" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-lg modal-dialog-centered">
    <div class="modal-content app-modal__content">

      <div class="app-modal__header">
        <h5 class="app-modal__title">Ver estado solicitud</h5>
        <button type="button" class="app-modal__close" data-bs-dismiss="modal" aria-label="Cerrar">x</button>
      </div>

      <div class="app-modal__body">
        <div class="row mb-2">
          <label class="col-sm-4 col-form-label fw-semibold">Fecha solicitud</label>
          <div class="col-sm-8">
            <input type="text" class="app-input" id="modal-fecha" placeholder=" " readonly>
          </div>
        </div>
        <div class="row mb-2">
          <label class="col-sm-4 col-form-label fw-semibold">N. Solicitud</label>
          <div class="col-sm-8">
            <input type="text" class="app-input" id="modal-numero" placeholder=" " readonly>
          </div>
        </div>
        <div class="row mb-3">
          <label class="col-sm-4 col-form-label fw-semibold">Producto / Servicio</label>
          <div class="col-sm-8">
            <input type="text" class="app-input" id="modal-producto" placeholder=" " readonly>
          </div>
        </div>

        <table class="table table-bordered">
          <thead class="table-light">
            <tr>
              <th class="text-center">Fecha estado</th>
              <th class="text-center">Estado</th>
              <th>Observaciones</th>
            </tr>
          </thead>
          <tbody id="tbody-seguimiento">
          </tbody>
        </table>
      </div>

      <div class="app-modal__footer justify-content-center">
        <button type="button" class="app-button" data-bs-dismiss="modal">Cerrar</button>
      </div>

    </div>
  </div>
</div>
